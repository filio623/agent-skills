---
name: skill-name
description: Use this skill when an AI agent needs to do a specific recurring task. Replace this with clear trigger conditions, key domains, and expected outcomes.
---

# Skill Name

## When To Use

Use this skill when:

- The user asks for this specific workflow or domain.
- The task requires knowledge or steps that should be reused across projects.

Do not use this skill when:

- The task is generic enough for normal agent behavior.
- Another more specific skill applies.

## Workflow

1. Identify the user's goal and relevant constraints.
2. Inspect local project context before making assumptions.
3. Apply the workflow, scripts, or references bundled with this skill.
4. Validate the result using the checks appropriate for the task.
5. Summarize the outcome and any remaining risks.

## References

Load files from `references/` only when needed for the task.

## Scripts

Run scripts from `scripts/` when deterministic behavior is required.
